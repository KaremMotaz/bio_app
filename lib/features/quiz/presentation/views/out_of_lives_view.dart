import 'package:flutter/material.dart';

class OutOfLivesView extends StatelessWidget {
  const OutOfLivesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Out Of Lives View')));
  }
}
