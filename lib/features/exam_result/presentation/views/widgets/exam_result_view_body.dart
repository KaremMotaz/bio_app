import 'package:flutter/material.dart';

class ExamResultViewBody extends StatelessWidget {
  const ExamResultViewBody({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Result"),
    );
  }
}